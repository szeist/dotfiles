export ZSH=$HOME/.oh-my-zsh

ZSH_THEME="ys"
HIST_STAMPS="yyyy-mm-dd"

plugins=(adb bower bundler cabal capistrano composer django docker docker-compose gem git gulp heroku httpie nmap node npm nvm pip python rake ruby rvm ssh-agent systemd tmux vagrant virtualenvwrapper)

source $ZSH/oh-my-zsh.sh

